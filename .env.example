# ================================
# ekaya-engine Secrets
# ================================
# Optional: Copy to .env and source before running the server.
# These can also be set directly in your shell or passed to the process.
#
#   cp .env.example .env
#   source .env

# PostgreSQL password for local database access
export PGPASSWORD=

# 32-byte key for encrypting project credentials (AI keys, database passwords, etc.)
# Generate with: openssl rand -base64 32
# NOTE: Cannot be changed after projects are created - would invalidate stored credentials
export PROJECT_CREDENTIALS_KEY=
